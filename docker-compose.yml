version: '3.8'

services:
  web:
    build: .
    container_name: ringmc
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/src/static/uploads
    env_file:
      - .env